{
    // See https://go.microsoft.com/fwlink/?LinkId=733558 
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "load and run qemu",
            "command": "./runqemu_uefi",
            "args": [
                "64",
                "load"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": "test"
        },
        {
            "type": "shell",
            "label": "run qemu",
            "command": "./runqemu_uefi",
            "args": [
                "64"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": "test"
        }
    ]
}